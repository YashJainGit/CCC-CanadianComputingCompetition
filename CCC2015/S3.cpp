include <bits/stdc++.h>

using namespace std;


struct nx{
    int b, t, h;
};

int k, n, m;
vector<vector<nx>> islands;
int a, b;

void solve(){
    cin >> k >> n >> m;
    islands.resize(n);

    for (int i=0; i<n; i++){

        
    }
}


int main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout<<fixed<<setprecision(0);

    solve();

    return 0;
}